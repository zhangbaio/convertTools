<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="ShortDrama.Desktop.Views.ManualMaterialProjectWindow"
        Width="680"
        Height="340"
        CanResize="False"
        Title="手动创建素材项目">
    <Grid Margin="20"
          RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto"
          ColumnDefinitions="120,*,Auto"
          RowSpacing="12"
          ColumnSpacing="12">
        <TextBlock Grid.ColumnSpan="3"
                   Text="适用于原项目已删除或工作目录没有该剧的场景：有本地视频时会导入视频；没有视频时会创建新剧挂载项目，运行时按剧名自动搜索并下载。"
                   TextWrapping="Wrap" />

        <TextBlock Grid.Row="1"
                   VerticalAlignment="Center"
                   Text="视频目录" />
        <TextBox x:Name="VideoDirectoryTextBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 IsReadOnly="True"
                 Watermark="选择视频目录；空目录会作为新剧挂载项目" />
        <Button x:Name="BrowseButton"
                Grid.Row="1"
                Grid.Column="2"
                Content="浏览..." />

        <TextBlock Grid.Row="2"
                   VerticalAlignment="Center"
                   Text="可用视频" />
        <TextBlock x:Name="VideoCountTextBlock"
                   Grid.Row="2"
                   Grid.Column="1"
                   Grid.ColumnSpan="2"
                   VerticalAlignment="Center"
                   Text="未选择" />

        <TextBlock Grid.Row="3"
                   VerticalAlignment="Center"
                   Text="新剧名" />
        <TextBox x:Name="NewTitleTextBox"
                 Grid.Row="3"
                 Grid.Column="1"
                 Grid.ColumnSpan="2"
                 Watermark="上传时使用的新剧名（必填）" />

        <TextBlock Grid.Row="4"
                   VerticalAlignment="Center"
                   Text="原剧名" />
        <TextBox x:Name="OriginalTitleTextBox"
                 Grid.Row="4"
                 Grid.Column="1"
                 Grid.ColumnSpan="2"
                 Watermark="可选，默认与新剧名相同" />

        <TextBlock Grid.Row="5"
                   VerticalAlignment="Center"
                   Text="集数" />
        <StackPanel Grid.Row="5"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    Spacing="12">
            <NumericUpDown x:Name="EpisodeCountUpDown"
                           Width="160"
                           Minimum="0"
                           Maximum="9999"
                           Value="0" />
            <TextBlock VerticalAlignment="Center"
                       Foreground="{DynamicResource DesktopTextSecondaryBrush}"
                       Text="0 表示自动（有视频按视频数，无视频按30次）" />
        </StackPanel>

        <StackPanel Grid.Row="6"
                    Grid.ColumnSpan="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="12">
            <Button x:Name="CancelButton"
                    Content="取消" />
            <Button x:Name="CreateButton"
                    Content="创建项目" />
        </StackPanel>
    </Grid>
</Window>
